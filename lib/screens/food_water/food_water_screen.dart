import 'package:flutter/material.dart';

class FoodWaterScreen extends StatelessWidget {
  const FoodWaterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Food + Water'),
    );
  }
}